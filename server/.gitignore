# Node / Vite
node_modules/
dist/
build/
*.log

# VSCode / OS
.vscode/
.DS_Store

# Env files (keep a sample)
.env
.env.*
!.env.example
!.env.sample

# Server envs (in case you keep per-folder)
server/.env
src/.env

# OCI / private keys
server/keys/**
!server/keys/.gitkeep   # keep folder structure only
*.pem
*.key
*.p12
*.pfx
*.cer
*.crt
*.der
*.pub

# Local config you don’t want in git
server/config/*.json
